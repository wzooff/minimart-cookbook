cookbooks:
<% @list.each do |item,data| %>
  <%= item %>:
    git:
      location: '<%= data['git']['location'] %>'
      tags:
      <% data['git']['tags'].each do |tag| %>
        - <%= tag %>
      <% end %>
<% end %>
